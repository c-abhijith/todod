import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/todod/', // Replace 'your-repository-name' with your actual GitHub repository name
});
